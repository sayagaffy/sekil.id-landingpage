# Sekil.id Landing Page — Repo Setup Guide v1.0

**Companion to:** `01_Landingpage_Spec_v1.md`, `02_SEO_AEO_GEO_Strategy.md`, `03_Content_Production_Pipeline.md`, `04_Claude_Design_Handoff_Prompts.md`
**Target repo:** https://github.com/PT-DART-PRIHADITAMA-STUDIO/sekil.id-landingpage
**Status:** Ready for execution
**Timeline:** Hari 1–3 untuk full bootstrap, lalu masuk Week 2 build pages.

---

## 0. Pre-Flight Checklist

Sebelum eksekusi, pastikan akses & asset siap:

- [ ] Repo `sekil.id-landingpage` ada di GitHub (✓ confirmed by founder)
- [ ] Anthropic API key (untuk Claude Code & content generation script)
- [ ] Akses Vercel account dengan permission untuk deploy
- [ ] DNS access untuk `sekil.id` (di registrar atau Cloudflare)
- [ ] GitHub admin permission untuk add Actions secrets, set branch protection
- [ ] Brand asset siap: logo SVG (dark + light variant), favicon source, OG image source
- [ ] Resend account (untuk transactional email) — atau alternatif (Postmark, SES)
- [ ] Cloudflare account (untuk Turnstile captcha, free tier)
- [ ] Google account untuk Search Console + Analytics 4

---

## 1. Stack Final (Locked)

```
Framework:        Next.js 14 (App Router)
Language:         TypeScript 5+ (strict)
Styling:          Tailwind CSS + shadcn/ui
Content:          MDX (blog, guides, programmatic) + JSON/TS (data)
CMS:              File-based V1 → Decap CMS V1.5 (free, git-based)
Forms:            Next.js API routes + Postgres + Resend
Database:         Vercel Postgres free tier V1 (migrate kalau scale)
Captcha:          Cloudflare Turnstile (free)
Analytics:        GA4 + Search Console + Vercel Analytics + Sentry
Hosting:          Vercel (V1)
DNS:              Cloudflare (recommended) atau registrar provider
Image CDN:        Vercel Image Optimization (built-in)
i18n:             next-intl (default id-ID, future-proof EN)
```

---

## 2. Repo Initialization

### 2.1 Bootstrap Next.js 14

```bash
# Clone repo
git clone https://github.com/PT-DART-PRIHADITAMA-STUDIO/sekil.id-landingpage.git
cd sekil.id-landingpage

# Initialize Next.js (overwrite kalau repo kosong)
npx create-next-app@latest . \
  --typescript \
  --tailwind \
  --app \
  --src-dir \
  --import-alias "@/*" \
  --use-npm \
  --no-eslint

# Install core dependencies
npm install \
  @next/mdx @mdx-js/loader @mdx-js/react \
  next-intl \
  next-sitemap \
  next-themes \
  zod \
  lucide-react \
  class-variance-authority clsx tailwind-merge \
  date-fns \
  resend \
  @prisma/client \
  @marsidev/react-turnstile

# Install dev dependencies
npm install -D \
  @types/node @types/react @types/react-dom \
  eslint eslint-config-next \
  prettier prettier-plugin-tailwindcss \
  @typescript-eslint/eslint-plugin @typescript-eslint/parser \
  prisma \
  tsx \
  tailwindcss-animate \
  @tailwindcss/typography

# Install shadcn/ui
npx shadcn-ui@latest init
# Pilihan saat prompt:
# - Style: Default
# - Base color: Slate (akan di-override via design tokens)
# - CSS variables: Yes

# Add initial shadcn components
npx shadcn-ui@latest add button card input label select textarea \
  accordion sheet dialog dropdown-menu badge separator tabs
```

### 2.2 Folder Structure

```
sekil.id-landingpage/
├── .github/
│   └── workflows/
│       ├── ci.yml                      # Build + lint + type-check
│       └── content-validate.yml        # YMYL content validation
├── public/
│   ├── robots.txt                      # Generated by next-sitemap
│   ├── llms.txt                        # AEO: site overview untuk LLM
│   ├── favicon.ico
│   ├── og-image.png                    # Default OG image
│   └── images/
│       ├── logos/
│       ├── products/
│       └── methodology/
├── content/
│   ├── blog/
│   │   ├── _index.json
│   │   └── *.mdx
│   ├── panduan/
│   │   └── *.mdx
│   ├── kepribadian/                    # Programmatic (generated)
│   │   └── *.mdx
│   ├── karier/                         # Programmatic (generated)
│   │   └── *.mdx
│   ├── jurusan/                        # Programmatic (generated)
│   │   └── *.mdx
│   ├── untuk/                          # Programmatic (manual + LLM)
│   │   └── *.mdx
│   └── legal/
│       ├── privacy.mdx
│       └── terms.mdx
├── src/
│   ├── app/
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   ├── tentang/page.tsx
│   │   ├── produk/
│   │   │   ├── page.tsx
│   │   │   └── [slug]/page.tsx
│   │   ├── solusi/
│   │   │   ├── page.tsx
│   │   │   └── [segment]/page.tsx
│   │   ├── harga/page.tsx
│   │   ├── metodologi/page.tsx
│   │   ├── demo/
│   │   │   ├── page.tsx
│   │   │   └── terimakasih/page.tsx
│   │   ├── kontak/page.tsx
│   │   ├── blog/
│   │   │   ├── page.tsx
│   │   │   └── [slug]/page.tsx
│   │   ├── panduan/
│   │   │   ├── page.tsx
│   │   │   └── [slug]/page.tsx
│   │   ├── kepribadian/[type]/page.tsx
│   │   ├── karier/[slug]/page.tsx
│   │   ├── jurusan/[slug]/page.tsx
│   │   ├── untuk/[vertical]/page.tsx
│   │   ├── privacy/page.tsx
│   │   ├── terms/page.tsx
│   │   ├── not-found.tsx
│   │   ├── sitemap.ts
│   │   ├── sitemap-programmatic.xml/route.ts
│   │   ├── robots.ts
│   │   └── api/
│   │       ├── demo-request/route.ts
│   │       ├── waitlist/route.ts
│   │       └── newsletter/route.ts
│   ├── components/
│   │   ├── ui/                         # shadcn base
│   │   ├── layout/                     # Header, Footer, Container
│   │   ├── marketing/                  # Hero, SocialProof, ProductShowcase
│   │   ├── product/                    # Product detail parts
│   │   ├── solution/
│   │   ├── pricing/
│   │   ├── methodology/
│   │   ├── demo/
│   │   ├── blog/
│   │   ├── programmatic/               # Shared programmatic layout
│   │   ├── mdx/                        # Custom MDX components
│   │   └── seo/                        # JsonLd, AuthorByline, DisclaimerBlock
│   ├── lib/
│   │   ├── seo/                        # Schema generators
│   │   ├── mdx/                        # MDX loader
│   │   ├── db/                         # Prisma client
│   │   ├── email/                      # Resend client + templates
│   │   ├── pricing/                    # Pricing calculator
│   │   ├── turnstile/                  # Captcha verification
│   │   ├── validation/                 # Zod schemas
│   │   └── utils.ts
│   ├── data/
│   │   ├── products.ts
│   │   ├── solutions.ts
│   │   ├── methodology.ts
│   │   ├── careers.ts
│   │   ├── majors.ts
│   │   ├── personality-types.ts
│   │   └── b-one-mapping.ts
│   ├── styles/
│   │   ├── globals.css
│   │   └── themes.css
│   ├── i18n/
│   │   ├── id.json
│   │   └── config.ts
│   └── middleware.ts
├── design/
│   ├── design-tokens.json              # Extracted from Claude Design
│   └── handoff/                        # Per-page design refs
├── prisma/
│   └── schema.prisma
├── scripts/
│   ├── generate-programmatic.ts        # LLM content generation
│   ├── validate-content.ts             # YMYL validation
│   └── seed-db.ts
├── specs/                              # Mirror of /mnt/project specs
│   ├── 01_Landingpage_Spec_v1.md
│   ├── 02_SEO_AEO_GEO_Strategy.md
│   ├── 03_Content_Production_Pipeline.md
│   └── 04_Claude_Design_Handoff_Prompts.md
├── .env.example
├── .env.local                          # Git-ignored
├── .gitignore
├── .prettierrc
├── .eslintrc.json
├── CLAUDE.md
├── README.md
├── next.config.mjs
├── tailwind.config.ts
├── tsconfig.json
└── package.json
```

### 2.3 CLAUDE.md (Project Convention)

Buat di root sebagai single-source-of-truth untuk Claude Code/Cursor/Copilot:

```markdown
# Sekil.id Landing Page — Project Convention

## Overview
Marketing landing page untuk Sekil.id (JV Sekil.id × B One Corp).
- Production: https://sekil.id
- Portal app (separate repo): app.sekil.id

## Tech Stack
- Next.js 14 App Router
- TypeScript strict mode
- Tailwind CSS + shadcn/ui
- MDX untuk content
- Vercel deployment

## Conventions

### TypeScript
- Strict mode enabled
- No `any` allowed
- Explicit interface untuk component props
- Type imports separate (`import type { Foo } from ...`)

### File Naming
- Components: PascalCase (`ProductCard.tsx`)
- Utilities: kebab-case (`get-products.ts`)
- Pages: lowercase (`page.tsx`, `layout.tsx`)
- MDX: kebab-case slug

### Component Patterns
- Server Component default (App Router)
- Client Component hanya kalau perlu interactivity (`'use client'`)
- Page-specific component di folder yang sama dengan page
- Shared component di `src/components/{category}/`

### Styling
- Tailwind utility classes
- Custom values via CSS variables di `src/styles/globals.css`
- Design tokens di `design/design-tokens.json` (single source)
- DO NOT hardcode color/spacing — gunakan token

### SEO
- Setiap page WAJIB: title, description, canonical, JSON-LD schema
- Schema generator di `src/lib/seo/`
- Use Next.js Metadata API (`export const metadata`)

### Content
- Blog/guide content di `content/*.mdx`
- Programmatic content generated via script (lihat specs/03)
- Setiap MDX file ada frontmatter: title, description, publishedAt, modifiedAt, author

### Accessibility
- WCAG AA compliance
- Semantic HTML
- Keyboard navigable
- Alt text descriptive

### Performance
- Next.js Image untuk semua image
- Font subset + preload critical
- Lazy load below-fold
- Core Web Vitals green target

## Forbidden
- Hardcoded color hex / px value (use tokens)
- `any` type
- Inline event handler tanpa client component declaration
- External CSS file selain `globals.css` + `themes.css`
- `<img>` tag (use Next.js Image)
- `<a>` tag untuk internal link (use Next.js Link)

## Reference Docs
- specs/01_Landingpage_Spec_v1.md — full page spec
- specs/02_SEO_AEO_GEO_Strategy.md — SEO technical
- specs/03_Content_Production_Pipeline.md — content generation
- specs/04_Claude_Design_Handoff_Prompts.md — handoff prompts

## Workflow
1. Always read relevant spec doc sebelum start page
2. Pull latest design tokens dari Claude Design
3. Build dengan reuse existing component
4. Test mobile (375px) sebelum claim done
5. Run `npm run build` pass sebelum PR
6. Side-by-side compare dengan design canvas
```

### 2.4 .env.example

```bash
# Database
DATABASE_URL="postgresql://user:password@host:5432/sekil_marketing"

# Email (Resend)
RESEND_API_KEY=""
RESEND_FROM="hello@sekil.id"
SALES_NOTIFICATION_EMAIL="sales@sekil.id"

# Captcha (Cloudflare Turnstile)
NEXT_PUBLIC_TURNSTILE_SITE_KEY=""
TURNSTILE_SECRET_KEY=""

# Analytics
NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXX"

# Sentry
SENTRY_DSN=""
NEXT_PUBLIC_SENTRY_DSN=""

# Anthropic API (untuk content generation script)
ANTHROPIC_API_KEY=""

# Site
NEXT_PUBLIC_SITE_URL="https://sekil.id"

# Environment
NODE_ENV="development"
```

### 2.5 .gitignore Additions

```
# Next.js
.next/
out/

# Environment
.env.local
.env.*.local

# Database
*.db
*.db-journal

# Sentry
.sentryclirc

# Vercel
.vercel/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Generated
public/sitemap.xml
public/sitemap-0.xml
public/robots.txt

# Content drafts
content/_drafts/

# Prisma generated
src/generated/
```

---

## 3. Configuration Files

### 3.1 next.config.mjs

```javascript
import createMDX from '@next/mdx';

/** @type {import('next').NextConfig} */
const nextConfig = {
  pageExtensions: ['ts', 'tsx', 'mdx'],
  experimental: { mdxRs: false },
  images: {
    formats: ['image/avif', 'image/webp'],
    remotePatterns: [],
  },
  async headers() {
    return [{
      source: '/(.*)',
      headers: [
        { key: 'Strict-Transport-Security', value: 'max-age=63072000; includeSubDomains; preload' },
        { key: 'X-Content-Type-Options', value: 'nosniff' },
        { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
        { key: 'Permissions-Policy', value: 'camera=(), microphone=(), geolocation=()' },
        { key: 'X-Frame-Options', value: 'SAMEORIGIN' },
      ],
    }];
  },
  async redirects() {
    return [{
      source: '/:path*',
      has: [{ type: 'host', value: 'www.sekil.id' }],
      destination: 'https://sekil.id/:path*',
      permanent: true,
    }];
  },
};

const withMDX = createMDX({
  extension: /\.mdx?$/,
  options: { remarkPlugins: [], rehypePlugins: [] },
});

export default withMDX(nextConfig);
```

### 3.2 tailwind.config.ts

```typescript
import type { Config } from 'tailwindcss';

const config: Config = {
  content: [
    './src/**/*.{ts,tsx,js,jsx,mdx}',
    './content/**/*.{md,mdx}',
  ],
  theme: {
    container: {
      center: true,
      padding: { DEFAULT: '1rem', sm: '1.5rem', lg: '2rem' },
      screens: { '2xl': '1280px' },
    },
    extend: {
      colors: {
        primary: {
          50: 'hsl(var(--color-primary-50))',
          500: 'hsl(var(--color-primary-500))',
          950: 'hsl(var(--color-primary-950))',
          DEFAULT: 'hsl(var(--primary))',
          foreground: 'hsl(var(--primary-foreground))',
        },
        accent: {
          DEFAULT: 'hsl(var(--accent))',
          foreground: 'hsl(var(--accent-foreground))',
        },
        background: 'hsl(var(--background))',
        foreground: 'hsl(var(--foreground))',
        card: {
          DEFAULT: 'hsl(var(--card))',
          foreground: 'hsl(var(--card-foreground))',
        },
        border: 'hsl(var(--border))',
        input: 'hsl(var(--input))',
        ring: 'hsl(var(--ring))',
        muted: {
          DEFAULT: 'hsl(var(--muted))',
          foreground: 'hsl(var(--muted-foreground))',
        },
      },
      fontFamily: {
        sans: ['var(--font-sans)', 'system-ui', 'sans-serif'],
        display: ['var(--font-display)', 'var(--font-sans)', 'sans-serif'],
      },
      borderRadius: {
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)',
      },
    },
  },
  plugins: [require('tailwindcss-animate'), require('@tailwindcss/typography')],
};

export default config;
```

### 3.3 src/styles/globals.css

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 222 47% 11%;
    --card: 0 0% 100%;
    --card-foreground: 222 47% 11%;

    /* Primary navy from Sekil.id deck — placeholder, akan di-override */
    --color-primary-50: 210 100% 96%;
    --color-primary-500: 217 71% 22%;
    --color-primary-950: 222 47% 11%;
    --primary: 217 71% 22%;
    --primary-foreground: 0 0% 100%;

    /* Accent yellow */
    --accent: 47 100% 62%;
    --accent-foreground: 222 47% 11%;

    --border: 214 32% 91%;
    --input: 214 32% 91%;
    --ring: 217 71% 22%;
    --muted: 210 40% 96%;
    --muted-foreground: 215 16% 47%;

    --radius: 0.5rem;

    --font-sans: 'Inter', system-ui, sans-serif;
    --font-display: 'Inter', system-ui, sans-serif;
  }

  .dark {
    --background: 222 47% 11%;
    --foreground: 0 0% 100%;
  }
}

@layer base {
  * { @apply border-border; }
  body {
    @apply bg-background text-foreground;
    font-feature-settings: 'rlig' 1, 'calt' 1;
  }
}

/* Dot pattern utility — branded from Sekil.id deck */
.bg-dot-pattern {
  background-image: radial-gradient(
    circle,
    hsl(var(--foreground) / 0.08) 1px,
    transparent 1px
  );
  background-size: 24px 24px;
}
```

### 3.4 next-sitemap.config.js

```javascript
/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: process.env.NEXT_PUBLIC_SITE_URL || 'https://sekil.id',
  generateRobotsTxt: true,
  generateIndexSitemap: true,
  exclude: ['/api/*', '/admin/*', '/demo/terimakasih'],
  changefreq: 'weekly',
  priority: 0.7,
  robotsTxtOptions: {
    policies: [
      { userAgent: '*', allow: '/' },
      { userAgent: '*', disallow: ['/api/', '/admin/'] },
      { userAgent: 'GPTBot', allow: '/' },
      { userAgent: 'ClaudeBot', allow: '/' },
      { userAgent: 'PerplexityBot', allow: '/' },
      { userAgent: 'Google-Extended', allow: '/' },
    ],
    additionalSitemaps: ['https://sekil.id/sitemap-programmatic.xml'],
  },
  transform: async (config, path) => {
    let priority = 0.7;
    if (path === '/') priority = 1.0;
    else if (['/produk', '/solusi', '/harga', '/metodologi'].includes(path)) priority = 0.8;
    else if (path.startsWith('/blog/') || path.startsWith('/panduan/')) priority = 0.7;
    else if (
      path.startsWith('/kepribadian/') ||
      path.startsWith('/karier/') ||
      path.startsWith('/jurusan/')
    ) priority = 0.5;

    return {
      loc: path,
      changefreq: config.changefreq,
      priority,
      lastmod: new Date().toISOString(),
    };
  },
};
```

### 3.5 tsconfig.json

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noImplicitAny": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "baseUrl": ".",
    "paths": { "@/*": ["./src/*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

### 3.6 .prettierrc

```json
{
  "semi": true,
  "singleQuote": true,
  "trailingComma": "es5",
  "tabWidth": 2,
  "printWidth": 100,
  "plugins": ["prettier-plugin-tailwindcss"]
}
```

### 3.7 .eslintrc.json

```json
{
  "extends": ["next/core-web-vitals"],
  "rules": {
    "@typescript-eslint/no-explicit-any": "error",
    "@typescript-eslint/no-unused-vars": "warn",
    "react/no-unescaped-entities": "off"
  }
}
```

---

## 4. CI/CD Setup

### 4.1 GitHub Actions — Build & Lint

`.github/workflows/ci.yml`:

```yaml
name: CI

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Install dependencies
        run: npm ci
      - name: Type check
        run: npm run type-check
      - name: Lint
        run: npm run lint
      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_SITE_URL: https://sekil.id
          DATABASE_URL: postgresql://placeholder
          RESEND_API_KEY: placeholder
          NEXT_PUBLIC_TURNSTILE_SITE_KEY: placeholder
          TURNSTILE_SECRET_KEY: placeholder
```

### 4.2 GitHub Actions — Content Validation (YMYL)

`.github/workflows/content-validate.yml`:

```yaml
name: Content Validate (YMYL)

on:
  pull_request:
    paths:
      - 'content/**/*.mdx'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - name: Validate content
        run: npm run content:validate
```

### 4.3 package.json Scripts

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "type-check": "tsc --noEmit",
    "format": "prettier --write .",
    "postbuild": "next-sitemap",
    "content:generate": "tsx scripts/generate-programmatic.ts",
    "content:validate": "tsx scripts/validate-content.ts",
    "db:migrate": "prisma migrate dev",
    "db:generate": "prisma generate",
    "db:seed": "tsx scripts/seed-db.ts"
  }
}
```

### 4.4 Branch Protection (GitHub Settings)

Di GitHub repo → Settings → Branches → Add rule untuk `main`:

- [x] Require pull request before merging
- [x] Require approvals (minimum 1)
- [x] Dismiss stale approvals when new commits pushed
- [x] Require status checks to pass before merging
  - Required: `build` (dari ci.yml)
- [x] Require branches to be up to date before merging
- [x] Do not allow bypassing the above settings

Repeat untuk `develop` dengan setting yang lebih longgar (no approval required, tapi CI must pass).

---

## 5. Database Setup

### 5.1 Vercel Postgres (Free Tier V1)

1. Vercel Dashboard → Storage → Create → Postgres
2. Pilih region: Singapore (sin1) — closest to Indonesia
3. Copy `DATABASE_URL` ke environment variables
4. Free tier: 256MB storage, 60 compute-hours/bulan — sufficient untuk V1 (form lead volume rendah)

### 5.2 Prisma Schema

`prisma/schema.prisma`:

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Lead {
  id                  String      @id @default(cuid())
  fullName            String
  email               String
  whatsapp            String
  institutionName     String
  institutionType     String
  estimatedSeats      String
  interestedProducts  String[]
  message             String?
  source              String?
  medium              String?
  campaign            String?
  referrer            String?
  segment             String?
  productFromUrl      String?
  status              LeadStatus  @default(NEW)
  assignedTo          String?
  notes               String?
  createdAt           DateTime    @default(now())
  updatedAt           DateTime    @updatedAt

  @@index([status, createdAt])
  @@index([email])
}

enum LeadStatus {
  NEW
  CONTACTED
  QUALIFIED
  DEMO_SCHEDULED
  PROPOSAL_SENT
  CLOSED_WON
  CLOSED_LOST
}

model WaitlistEntry {
  id              String    @id @default(cuid())
  email           String    @unique
  segment         String?
  source          String?
  referrer        String?
  createdAt       DateTime  @default(now())
  unsubscribedAt  DateTime?
}

model NewsletterSubscriber {
  id              String    @id @default(cuid())
  email           String    @unique
  confirmedAt     DateTime?
  unsubscribedAt  DateTime?
  topics          String[]  @default([])
  createdAt       DateTime  @default(now())
}
```

Initialize:

```bash
npx prisma migrate dev --name init
npx prisma generate
```

### 5.3 Prisma Client Singleton

`src/lib/db/index.ts`:

```typescript
import { PrismaClient } from '@prisma/client';

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined;
};

export const prisma = globalForPrisma.prisma ?? new PrismaClient({
  log: process.env.NODE_ENV === 'development' ? ['query', 'error', 'warn'] : ['error'],
});

if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma;
```

---

## 6. Email Service Setup (Resend)

### 6.1 Account & Domain Verification

1. https://resend.com → sign up
2. Add domain `sekil.id` → Resend kasih DNS records (DKIM, SPF, DMARC)
3. Tambah records ke DNS provider (Cloudflare) — propagasi 24–72 jam
4. Verify domain di Resend dashboard
5. Generate API key → env `RESEND_API_KEY`

### 6.2 Resend Client

`src/lib/email/client.ts`:

```typescript
import { Resend } from 'resend';

export const resend = new Resend(process.env.RESEND_API_KEY);

export const EMAIL_FROM = process.env.RESEND_FROM || 'hello@sekil.id';
export const SALES_EMAIL = process.env.SALES_NOTIFICATION_EMAIL || 'sales@sekil.id';
```

### 6.3 Email Templates

`src/lib/email/templates/demo-request-internal.ts`:

```typescript
import type { Lead } from '@prisma/client';

export function demoRequestInternalEmail(lead: Lead): string {
  return `
    <h2>Demo Request Baru</h2>
    <p><strong>Dari:</strong> ${lead.fullName} (${lead.email})</p>
    <p><strong>WhatsApp:</strong> ${lead.whatsapp}</p>
    <p><strong>Institusi:</strong> ${lead.institutionName} (${lead.institutionType})</p>
    <p><strong>Estimasi peserta:</strong> ${lead.estimatedSeats}</p>
    <p><strong>Produk yang menarik:</strong> ${lead.interestedProducts.join(', ') || '—'}</p>
    <p><strong>Pesan:</strong></p>
    <blockquote>${lead.message || '—'}</blockquote>
    <hr/>
    <p><small>Source: ${lead.source || '—'} / ${lead.medium || '—'} / ${lead.campaign || '—'}</small></p>
    <p><small>Referrer: ${lead.referrer || '—'}</small></p>
    <p><small>Lead ID: ${lead.id}</small></p>
    <p><strong>Action:</strong> Hubungi via WhatsApp dalam 24 jam kerja.</p>
  `;
}
```

`src/lib/email/templates/demo-request-confirmation.ts`:

```typescript
import type { Lead } from '@prisma/client';

export function demoRequestConfirmationEmail(lead: Lead): string {
  return `
    <h2>Terima kasih, ${lead.fullName}!</h2>
    <p>Kami sudah menerima permintaan demo Anda untuk <strong>${lead.institutionName}</strong>.</p>
    <p>Tim Sekil.id (didukung B One Corp) akan menghubungi Anda via WhatsApp atau email
    dalam 24 jam kerja untuk diskusi kebutuhan dan jadwalkan walkthrough.</p>
    <h3>Sambil menunggu, Anda bisa eksplor:</h3>
    <ul>
      <li><a href="https://sekil.id/produk">Katalog produk asesmen kami</a></li>
      <li><a href="https://sekil.id/metodologi">Metodologi & validasi akademik UNJANI</a></li>
      <li><a href="https://sekil.id/blog">Blog & insight karier</a></li>
    </ul>
    <p>Salam,<br/>Tim Sekil.id</p>
    <hr/>
    <p><small>Email ini dikirim otomatis. Untuk respon cepat, balas email ini atau hubungi WhatsApp +62-xxx-xxxx.</small></p>
  `;
}
```

---

## 7. Captcha Setup (Cloudflare Turnstile)

### 7.1 Turnstile Site Setup

1. https://dash.cloudflare.com → Turnstile → Add Site
2. Domain: `sekil.id`, `www.sekil.id`, dan `*.vercel.app` (preview deployment)
3. Widget mode: **Managed** (recommended, paling user-friendly)
4. Copy Site Key → env `NEXT_PUBLIC_TURNSTILE_SITE_KEY`
5. Copy Secret Key → env `TURNSTILE_SECRET_KEY`

### 7.2 Server-Side Verification

`src/lib/turnstile/verify.ts`:

```typescript
export async function verifyTurnstile(token: string): Promise<boolean> {
  const secret = process.env.TURNSTILE_SECRET_KEY;
  if (!secret) {
    console.error('TURNSTILE_SECRET_KEY not set');
    return false;
  }

  try {
    const response = await fetch(
      'https://challenges.cloudflare.com/turnstile/v0/siteverify',
      {
        method: 'POST',
        headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
        body: new URLSearchParams({ secret, response: token }),
      }
    );
    const data = await response.json();
    return data.success === true;
  } catch (error) {
    console.error('Turnstile verification failed:', error);
    return false;
  }
}
```

### 7.3 Client Component

`src/components/demo/TurnstileWidget.tsx`:

```typescript
'use client';

import { Turnstile } from '@marsidev/react-turnstile';

interface TurnstileWidgetProps {
  onSuccess: (token: string) => void;
  onError?: () => void;
}

export function TurnstileWidget({ onSuccess, onError }: TurnstileWidgetProps) {
  return (
    <Turnstile
      siteKey={process.env.NEXT_PUBLIC_TURNSTILE_SITE_KEY!}
      onSuccess={onSuccess}
      onError={onError}
      options={{ theme: 'light', size: 'normal' }}
    />
  );
}
```

---

## 8. Demo Request API Implementation

### 8.1 Validation Schema

`src/lib/validation/demo-schema.ts`:

```typescript
import { z } from 'zod';

export const demoSchema = z.object({
  fullName: z.string().min(2, 'Nama minimal 2 karakter').max(100),
  email: z.string().email('Format email tidak valid'),
  whatsapp: z.string().regex(/^\+?[0-9]{10,15}$/, 'Format nomor WhatsApp tidak valid'),
  institutionName: z.string().min(2).max(150),
  institutionType: z.enum(['SMA', 'SMK', 'PT_NEGERI', 'PT_SWASTA', 'PERUSAHAAN', 'YAYASAN', 'LAINNYA']),
  estimatedSeats: z.enum(['<100', '100-500', '500-2000', '2000-5000', '>5000']),
  interestedProducts: z.array(z.string()).default([]),
  message: z.string().max(500).optional(),
  turnstileToken: z.string().min(1, 'Captcha required'),
  utm_source: z.string().optional(),
  utm_medium: z.string().optional(),
  utm_campaign: z.string().optional(),
  referrer: z.string().optional(),
});

export type DemoFormData = z.infer<typeof demoSchema>;
```

### 8.2 API Route

`src/app/api/demo-request/route.ts`:

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { prisma } from '@/lib/db';
import { demoSchema } from '@/lib/validation/demo-schema';
import { verifyTurnstile } from '@/lib/turnstile/verify';
import { resend, EMAIL_FROM, SALES_EMAIL } from '@/lib/email/client';
import { demoRequestInternalEmail } from '@/lib/email/templates/demo-request-internal';
import { demoRequestConfirmationEmail } from '@/lib/email/templates/demo-request-confirmation';

export async function POST(req: NextRequest) {
  try {
    const body = await req.json();
    const parsed = demoSchema.parse(body);

    // Verify Turnstile
    const captchaOk = await verifyTurnstile(parsed.turnstileToken);
    if (!captchaOk) {
      return NextResponse.json({ error: 'Captcha verification failed' }, { status: 400 });
    }

    // Save to DB
    const lead = await prisma.lead.create({
      data: {
        fullName: parsed.fullName,
        email: parsed.email,
        whatsapp: parsed.whatsapp,
        institutionName: parsed.institutionName,
        institutionType: parsed.institutionType,
        estimatedSeats: parsed.estimatedSeats,
        interestedProducts: parsed.interestedProducts,
        message: parsed.message,
        source: parsed.utm_source,
        medium: parsed.utm_medium,
        campaign: parsed.utm_campaign,
        referrer: parsed.referrer,
      },
    });

    // Send emails parallel — don't fail request kalau email error
    await Promise.allSettled([
      resend.emails.send({
        from: EMAIL_FROM,
        to: SALES_EMAIL,
        subject: `[Demo Request] ${lead.institutionName} — ${lead.estimatedSeats} seats`,
        html: demoRequestInternalEmail(lead),
      }),
      resend.emails.send({
        from: EMAIL_FROM,
        to: lead.email,
        subject: 'Permintaan Demo Sekil.id Diterima',
        html: demoRequestConfirmationEmail(lead),
      }),
    ]);

    return NextResponse.json({ success: true, leadId: lead.id });
  } catch (error) {
    console.error('Demo request error:', error);
    if (error instanceof Error && error.name === 'ZodError') {
      return NextResponse.json({ error: 'Validation failed', details: error.message }, { status: 400 });
    }
    return NextResponse.json({ error: 'Submission failed' }, { status: 500 });
  }
}
```

---

## 9. Vercel Deployment

### 9.1 Project Setup

1. Login Vercel via GitHub
2. Import `PT-DART-PRIHADITAMA-STUDIO/sekil.id-landingpage`
3. Framework Preset: Next.js (auto-detected)
4. Root Directory: `.`
5. Build Command: `npm run build` (default)
6. Output Directory: `.next` (default)
7. Install Command: `npm ci`

### 9.2 Environment Variables

Vercel Dashboard → Settings → Environment Variables. Set per environment (Production, Preview, Development):

| Variable | Production | Preview | Notes |
|---|---|---|---|
| `DATABASE_URL` | Vercel Postgres prod | Vercel Postgres staging atau shared | — |
| `RESEND_API_KEY` | live key | test key | — |
| `RESEND_FROM` | `hello@sekil.id` | `hello@sekil.id` | — |
| `SALES_NOTIFICATION_EMAIL` | `sales@sekil.id` | own email (test) | — |
| `NEXT_PUBLIC_TURNSTILE_SITE_KEY` | live | live | Same untuk preview |
| `TURNSTILE_SECRET_KEY` | live | live | — |
| `NEXT_PUBLIC_GA_MEASUREMENT_ID` | live | (kosong / test) | Hindari pollute analytics dari preview |
| `SENTRY_DSN` | live | live | — |
| `NEXT_PUBLIC_SENTRY_DSN` | live | live | — |
| `ANTHROPIC_API_KEY` | — | — | Hanya untuk script lokal, jangan di Vercel kecuali ada API route |
| `NEXT_PUBLIC_SITE_URL` | `https://sekil.id` | preview URL | — |

### 9.3 Domain Setup

**Vercel Dashboard → Settings → Domains:**
1. Add `sekil.id` (apex)
2. Add `www.sekil.id` → set as redirect ke `sekil.id`

**DNS provider (Cloudflare recommended):**

```
Type    Name    Value                       TTL     Proxy
A       @       76.76.21.21                 Auto    DNS only (atau proxy)
CNAME   www     cname.vercel-dns.com        Auto    DNS only

# Untuk app subdomain (terpisah, point ke Go backend portal)
A       app     <Oracle Cloud IP>           Auto    DNS only
```

Vercel auto-provision SSL via Let's Encrypt. Propagasi ~5–30 menit.

**Verification:**
```bash
dig sekil.id +short
dig www.sekil.id +short
curl -I https://sekil.id  # should return 200
curl -I https://www.sekil.id  # should return 301 redirect ke sekil.id
```

### 9.4 Branch & Deployment Strategy

```
main branch       → production (sekil.id)
develop branch    → staging (Vercel preview persistent atau staging.sekil.id)
feature/*         → Vercel preview deployment per PR
```

**PR workflow:**
1. Feature branch dari `develop`
2. PR ke `develop` → preview URL generate otomatis
3. Reviewer (founder + tech lead) review preview
4. Merge → deploy ke staging
5. Manual cut release: merge `develop` → `main` → production deploy

**Hotfix:**
- Direct PR ke `main` untuk critical fix
- Cherry-pick back ke `develop` setelah merge

---

## 10. Analytics & Monitoring Setup

### 10.1 Google Analytics 4

1. https://analytics.google.com → create property "Sekil.id"
2. Property setting → Data Streams → Web → register `sekil.id`
3. Copy Measurement ID `G-XXXXXXX` → env `NEXT_PUBLIC_GA_MEASUREMENT_ID`
4. Implement di root layout:

```tsx
// src/app/layout.tsx (snippet)
import Script from 'next/script';

const GA_ID = process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID;

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="id">
      <body>
        {children}
        {GA_ID && (
          <>
            <Script
              src={`https://www.googletagmanager.com/gtag/js?id=${GA_ID}`}
              strategy="afterInteractive"
            />
            <Script id="ga-init" strategy="afterInteractive">
              {`
                window.dataLayer = window.dataLayer || [];
                function gtag(){dataLayer.push(arguments);}
                gtag('js', new Date());
                gtag('config', '${GA_ID}', { anonymize_ip: true });
              `}
            </Script>
          </>
        )}
      </body>
    </html>
  );
}
```

**Cookie consent**: GA tracking only after user consent — implement banner di Week 1 (lihat section 12.2 untuk UU PDP compliance).

### 10.2 Google Search Console

1. https://search.google.com/search-console → Add property
2. Pilih **Domain property**: `sekil.id` (most authoritative, covers all subdomain)
3. Verify via DNS TXT record (Cloudflare → add TXT record provided)
4. Wait propagasi (~10 menit) → verify
5. Submit sitemap:
   - `https://sekil.id/sitemap.xml`
   - `https://sekil.id/sitemap-programmatic.xml`
6. Setup email alert untuk indexing issue & Core Web Vitals problem

### 10.3 Sentry

1. https://sentry.io → create project Next.js
2. Run setup wizard: `npx @sentry/wizard@latest -i nextjs`
3. Wizard auto-create: `sentry.client.config.ts`, `sentry.server.config.ts`, `sentry.edge.config.ts`
4. Add DSN ke env vars (di Vercel)
5. Verify error tracking dengan trigger intentional error di staging

### 10.4 Vercel Analytics

Free tier enabled by default. Add ke root layout:

```tsx
import { Analytics } from '@vercel/analytics/react';
import { SpeedInsights } from '@vercel/speed-insights/next';

// Inside RootLayout
<Analytics />
<SpeedInsights />
```

Install:
```bash
npm install @vercel/analytics @vercel/speed-insights
```

### 10.5 Custom Event Tracking

Untuk track event di luar pageview (lihat spec 01 section 8.2):

`src/lib/analytics/events.ts`:

```typescript
declare global {
  interface Window {
    gtag?: (command: string, action: string, params?: Record<string, unknown>) => void;
  }
}

export function trackEvent(eventName: string, params: Record<string, unknown> = {}): void {
  if (typeof window === 'undefined') return;
  window.gtag?.('event', eventName, params);
}

// Pre-defined events
export const events = {
  demoFormStarted: () => trackEvent('demo_form_started'),
  demoFormSubmitted: (institutionType: string, seatEstimate: string) =>
    trackEvent('demo_form_submitted', { institution_type: institutionType, seat_estimate: seatEstimate }),
  waitlistSubmitted: (segment: string) => trackEvent('waitlist_submitted', { segment }),
  ctaClicked: (location: string, ctaText: string) =>
    trackEvent('cta_clicked', { location, cta_text: ctaText }),
  pricingCalculatorUsed: (price: number) =>
    trackEvent('pricing_calculator_used', { calculated_price: price }),
  productCardClicked: (slug: string) => trackEvent('product_card_clicked', { product_slug: slug }),
};
```

---

## 11. Content Validation Script

`scripts/validate-content.ts` (referenced di CI workflow):

```typescript
import { readdirSync, readFileSync, statSync } from 'node:fs';
import { join } from 'node:path';
import matter from 'gray-matter';

const CONTENT_ROOTS = [
  'content/blog',
  'content/panduan',
  'content/kepribadian',
  'content/karier',
  'content/jurusan',
];

const FORBIDDEN_PATTERNS = [
  // YMYL diagnostic claims
  /\b(diagnosis|kelainan|gangguan kepribadian)\b/i,
  // Overreach claims
  /\b(100%|pasti sukses|dijamin sukses|tidak akan pernah gagal)\b/i,
  // Medical advice
  /\b(harus minum obat|berhenti dari obat|terapi pasti)\b/i,
];

const REQUIRED_DISCLAIMER_PATTERNS = [
  /bukan diagnosis/i,
  /bukan pengganti konsultasi profesional/i,
  /hasil ini bersifat deskriptif/i,
];

const MIN_WORD_COUNT = 800;
const MAX_WORD_COUNT = 3500;

interface ValidationError {
  file: string;
  message: string;
  severity: 'error' | 'warning';
}

const errors: ValidationError[] = [];

function walkFiles(dir: string): string[] {
  const files: string[] = [];
  try {
    const entries = readdirSync(dir);
    for (const entry of entries) {
      const path = join(dir, entry);
      const stat = statSync(path);
      if (stat.isDirectory()) files.push(...walkFiles(path));
      else if (entry.endsWith('.mdx')) files.push(path);
    }
  } catch (e) {
    // Folder belum ada — skip
  }
  return files;
}

function validateFile(filepath: string): void {
  const raw = readFileSync(filepath, 'utf-8');
  const { data: frontmatter, content } = matter(raw);
  const wordCount = content.split(/\s+/).filter(Boolean).length;

  // Frontmatter required fields
  const requiredFields = ['title', 'description', 'publishedAt', 'author'];
  for (const field of requiredFields) {
    if (!frontmatter[field]) {
      errors.push({ file: filepath, message: `Missing frontmatter: ${field}`, severity: 'error' });
    }
  }

  // Word count
  if (wordCount < MIN_WORD_COUNT) {
    errors.push({
      file: filepath,
      message: `Word count ${wordCount} < min ${MIN_WORD_COUNT}`,
      severity: 'error',
    });
  }
  if (wordCount > MAX_WORD_COUNT) {
    errors.push({
      file: filepath,
      message: `Word count ${wordCount} > max ${MAX_WORD_COUNT}`,
      severity: 'warning',
    });
  }

  // Forbidden patterns
  for (const pattern of FORBIDDEN_PATTERNS) {
    if (pattern.test(content)) {
      errors.push({
        file: filepath,
        message: `Forbidden pattern match: ${pattern}`,
        severity: 'error',
      });
    }
  }

  // YMYL: must have disclaimer kalau content includes psychology terms
  const isYMYL = /\b(kepribadian|psikologi|asesmen|burnout|mental)\b/i.test(content);
  if (isYMYL) {
    const hasDisclaimer = REQUIRED_DISCLAIMER_PATTERNS.some((p) => p.test(content));
    if (!hasDisclaimer) {
      errors.push({
        file: filepath,
        message: 'YMYL content missing required disclaimer',
        severity: 'error',
      });
    }
  }
}

function main(): void {
  const allFiles = CONTENT_ROOTS.flatMap(walkFiles);
  console.log(`Validating ${allFiles.length} content files...`);
  allFiles.forEach(validateFile);

  const errCount = errors.filter((e) => e.severity === 'error').length;
  const warnCount = errors.filter((e) => e.severity === 'warning').length;

  if (errors.length === 0) {
    console.log('✓ All content valid');
    process.exit(0);
  }

  console.log(`\n${errCount} error(s), ${warnCount} warning(s):\n`);
  for (const e of errors) {
    const icon = e.severity === 'error' ? '✗' : '⚠';
    console.log(`${icon} ${e.file}: ${e.message}`);
  }

  if (errCount > 0) process.exit(1);
}

main();
```

Install dependency: `npm install -D gray-matter`

---

## 12. Compliance Setup

### 12.1 Privacy Policy & Terms (`content/legal/`)

Draft template di `content/legal/privacy.mdx`:

```mdx
---
title: "Kebijakan Privasi"
lastUpdated: "2026-05-16"
---

# Kebijakan Privasi Sekil.id

**Berlaku sejak:** 16 Mei 2026

## 1. Data yang Kami Kumpulkan

[Daftar: nama, email, WhatsApp, institusi, dll dari form demo request + waitlist]

## 2. Tujuan Penggunaan

[Penjelasan: untuk follow-up sales, notifikasi update produk, analytics agregat]

## 3. Penyimpanan & Retensi

[Lead data: disimpan 24 bulan. Newsletter: sampai unsubscribe.]

## 4. Sharing dengan Pihak Ketiga

[B One Corp (sebagai sales partner), penyedia infrastruktur (Vercel, Resend), tidak dijual.]

## 5. Hak Subjek Data

Sesuai UU Perlindungan Data Pribadi (UU 27/2022):
- Hak akses
- Hak koreksi
- Hak penghapusan
- Hak penarikan persetujuan

Kontak: privacy@sekil.id

## 6. Cookies & Tracking

[GA4 untuk analytics — opt-in via consent banner. Turnstile untuk anti-spam.]

## 7. Perubahan Kebijakan

[Notifikasi via email kalau ada perubahan material.]

## 8. Kontak

PT Dart Prihaditama Studio
[Alamat Bandung]
Email: privacy@sekil.id
```

**WAJIB**: review legal oleh pengacara sebelum production launch. Ini draft template untuk speedup proses.

### 12.2 Cookie Consent Banner

Implementasi minimal V1 (custom, no SaaS):

`src/components/legal/CookieConsent.tsx`:

```typescript
'use client';

import { useEffect, useState } from 'react';
import { Button } from '@/components/ui/button';

export function CookieConsent() {
  const [shown, setShown] = useState(false);

  useEffect(() => {
    const consent = localStorage.getItem('sekil-cookie-consent');
    if (!consent) setShown(true);
  }, []);

  const handleAccept = () => {
    localStorage.setItem('sekil-cookie-consent', 'accepted');
    localStorage.setItem('sekil-cookie-consent-date', new Date().toISOString());
    setShown(false);
    // Initialize GA setelah accept (deferred init pattern)
    window.dispatchEvent(new Event('consent-accepted'));
  };

  const handleReject = () => {
    localStorage.setItem('sekil-cookie-consent', 'rejected');
    setShown(false);
  };

  if (!shown) return null;

  return (
    <div className="fixed bottom-0 left-0 right-0 z-50 border-t bg-background p-4 shadow-lg md:p-6">
      <div className="container flex flex-col gap-4 md:flex-row md:items-center md:justify-between">
        <p className="text-sm text-muted-foreground">
          Kami menggunakan cookies untuk analytics dan meningkatkan pengalaman Anda.{' '}
          <a href="/privacy" className="underline">
            Pelajari lebih lanjut
          </a>
          .
        </p>
        <div className="flex gap-2">
          <Button variant="outline" onClick={handleReject}>
            Tolak
          </Button>
          <Button onClick={handleAccept}>Terima</Button>
        </div>
      </div>
    </div>
  );
}
```

Conditional GA init: load GA script hanya kalau consent accepted (improvement V1.5, V1 bisa pakai GA dengan `anonymize_ip` sebagai compromise).

### 12.3 llms.txt (AEO)

`public/llms.txt` (per spec doc 02 section 5.5):

```markdown
# Sekil.id

> Platform asesmen psikologi & pemetaan karier dengan validasi akademik UNJANI.
> Joint venture Sekil.id × B One Corp untuk pendidikan & korporasi di Indonesia.

## Produk Utama
- [Career Interest](https://sekil.id/produk/career-interest): Tes minat karier Holland Code, 15 menit, Rp 150.000/peserta
- [PsyAI](https://sekil.id/produk/psyai): Tes kepribadian terintegrasi MBTI + Holland, Rp 195.000
- [Path Finder AI](https://sekil.id/produk/path-finder-ai): Rekomendasi jurusan kuliah, Rp 150.000
- [Leadership Styles Test](https://sekil.id/produk/leadership-styles-test): Asesmen gaya kepemimpinan Papi Kostick, Rp 150.000
- [Emotional Intelligence Test](https://sekil.id/produk/emotional-intelligence-test): Tes EQ, Rp 175.000

## Metodologi
- Holland Code (RIASEC) — minat karier
- MBTI-style typing — dimensi kepribadian
- Papi Kostick — gaya kerja

Validasi akademik oleh Fakultas Psikologi UNJANI.

## Audience
- Sekolah Menengah Atas (SMA/SMK)
- Perguruan Tinggi (PT)
- Perusahaan & Korporasi
- Yayasan pendidikan multi-unit

## Kontak
Email: hello@sekil.id
Website: https://sekil.id
```

---

## 13. README.md

```markdown
# Sekil.id Landing Page

Marketing landing page untuk Sekil.id — platform asesmen psikologi & pemetaan karier.

- **Production**: https://sekil.id
- **Portal app** (separate repo): https://app.sekil.id

## Tech Stack
Next.js 14 App Router · TypeScript · Tailwind CSS · shadcn/ui · MDX · Vercel

## Getting Started

```bash
# Clone & install
git clone https://github.com/PT-DART-PRIHADITAMA-STUDIO/sekil.id-landingpage.git
cd sekil.id-landingpage
npm install

# Setup env
cp .env.example .env.local
# Fill in values

# Setup DB
npx prisma migrate dev
npx prisma generate

# Run dev
npm run dev
# → http://localhost:3000
```

## Scripts

| Command | Description |
|---|---|
| `npm run dev` | Dev server (with hot reload) |
| `npm run build` | Production build |
| `npm run start` | Run production build locally |
| `npm run lint` | ESLint check |
| `npm run type-check` | TypeScript check |
| `npm run format` | Prettier format |
| `npm run content:generate` | LLM generate programmatic content |
| `npm run content:validate` | Validate YMYL content compliance |
| `npm run db:migrate` | Run Prisma migration |
| `npm run db:seed` | Seed initial data |

## Project Structure

See `CLAUDE.md` for full conventions and folder map.

## Documentation

All specs in `specs/` directory (mirror of `/mnt/project`):
- `01_Landingpage_Spec_v1.md` — Full page-by-page spec
- `02_SEO_AEO_GEO_Strategy.md` — Technical SEO + AEO/GEO
- `03_Content_Production_Pipeline.md` — Programmatic content workflow
- `04_Claude_Design_Handoff_Prompts.md` — Page build prompts
- `05_Repo_Setup_Guide.md` — This file (in /mnt/project, copy here untuk reference)

## Deployment

- **Production**: auto-deploy from `main` branch via Vercel
- **Staging**: auto-deploy from `develop` branch
- **Preview**: auto-deploy per PR

## Contributing

1. Branch dari `develop`: `feature/{ticket}-{description}`
2. Commit message: conventional commits (`feat:`, `fix:`, `docs:`, `chore:`)
3. Open PR ke `develop`
4. CI must pass + 1 approval
5. Squash merge

## License

Proprietary — PT Dart Prihaditama Studio © 2026
```

---

## 14. Bootstrap Execution Order (Hari 1–3)

Sequence eksekusi dari nol sampai siap build pages:

### Hari 1 — Repo & Infra (4–6 jam)

1. **[30m]** Clone repo, run `create-next-app` bootstrap
2. **[30m]** Install dependencies, setup folder structure
3. **[30m]** Configure files: `next.config.mjs`, `tailwind.config.ts`, `tsconfig.json`, `.prettierrc`, `.eslintrc.json`, `.gitignore`
4. **[15m]** Buat `CLAUDE.md`, `README.md`, `.env.example`
5. **[30m]** Setup Prisma + schema + first migration
6. **[20m]** Setup Vercel Postgres → copy `DATABASE_URL`
7. **[30m]** Setup Resend → verify domain DNS records
8. **[20m]** Setup Cloudflare Turnstile → keys
9. **[20m]** Setup Sentry → wizard
10. **[15m]** Setup GA4 + GSC property
11. **[30m]** Commit initial, push, setup Vercel project, configure env vars
12. **[30m]** Setup DNS: `sekil.id` → Vercel, verify SSL
13. **[20m]** Setup GitHub Actions CI + branch protection
14. **[15m]** Smoke test: deploy hello world ke production, verify domain, SSL OK

### Hari 2 — Design Foundation (4–6 jam)

15. **[60m]** Run Claude Code dengan initial token extraction prompt (lihat doc 04 section 1.1)
16. **[60m]** Verify `design-tokens.json`, `globals.css`, `tailwind.config.ts` updated
17. **[60m]** Build base components: Button, Card, Input, Badge, FAQ accordion, Container
18. **[60m]** Build layout components: Header, Footer (placeholder content), Layout shell
19. **[30m]** Setup `<JsonLd>`, `<AuthorByline>`, `<DisclaimerBlock>` SEO components
20. **[30m]** Implement Cookie Consent banner

### Hari 3 — Content & API Skeleton (3–4 jam)

21. **[60m]** Implement `/api/demo-request` route + Turnstile verify + Resend send + DB save
22. **[30m]** Test end-to-end form submit (dev) → verify lead di DB + email delivered
23. **[30m]** Setup `next-sitemap` config + generate sitemap.xml
24. **[30m]** Buat `public/llms.txt`, `public/robots.txt` (auto-gen)
25. **[60m]** Implement content validation script + commit + verify CI green
26. **[30m]** Buat `data/products.ts`, `data/personality-types.ts`, `data/methodology.ts` skeleton (data seed Week 2)

**End Hari 3 state**: repo siap untuk Week 2 build foundation pages. Lihat doc 04 section 2 untuk prompt per page.

---

## 15. Launch Checklist

### 15.1 Pre-Launch (Akhir Week 4)

**Technical:**
- [ ] All foundation pages built & responsive (tested 375px, 768px, 1440px)
- [ ] `npm run build` pass
- [ ] `npm run lint` clean
- [ ] `npm run type-check` no errors
- [ ] Lighthouse audit all green: Performance ≥90, Accessibility ≥95, SEO 100, Best Practices ≥95
- [ ] Schema markup validated via https://validator.schema.org untuk top 10 page
- [ ] All internal links work (no 404)
- [ ] Form submit tested end-to-end production environment
- [ ] Turnstile working di production
- [ ] Email delivery confirmed (test send to real inbox, check spam folder)
- [ ] Sentry error tracking active (trigger test error)
- [ ] GA4 tracking events firing (DebugView)
- [ ] Vercel Analytics + Speed Insights collecting

**Content:**
- [ ] 5 product detail pages content reviewed by founder
- [ ] Methodology page UNJANI sign-off received (sign-off doc saved)
- [ ] 5 initial blog posts published
- [ ] 2 pillar guides published
- [ ] Privacy policy & terms reviewed by legal counsel
- [ ] No "lorem ipsum" or placeholder text remaining
- [ ] All images have descriptive alt text
- [ ] Author bios accurate dengan UNJANI credential

**SEO:**
- [ ] robots.txt accessible at `/robots.txt`
- [ ] sitemap.xml accessible & submitted ke GSC
- [ ] sitemap-programmatic.xml submitted ke GSC (kalau ready)
- [ ] Canonical URLs set correctly (test 3 random page)
- [ ] Meta description per page unique & ≤155 char
- [ ] Title per page unique & ≤60 char
- [ ] No `noindex` accidentally pada page yang seharusnya index
- [ ] OG image set default + override per page kunci
- [ ] llms.txt accessible at `/llms.txt`

**Branding:**
- [ ] Logo final & high-res (SVG dark + light)
- [ ] Favicon set (multiple sizes: 16, 32, 192, 512)
- [ ] OG image default set (1200×630)
- [ ] Color palette consistent across pages
- [ ] Brand voice consistent (spot-check 5 random page)

**Legal:**
- [ ] Privacy policy aligned UU PDP (UU 27/2022)
- [ ] Cookie consent banner active
- [ ] Terms of service reviewed
- [ ] Partner logo permission confirmed dengan dokumen (jika dipajang di homepage)
- [ ] Foto UNJANI lecturer permission confirmed

**Forms & Backend:**
- [ ] Production database backup configured (Vercel Postgres auto-backup)
- [ ] Sales notification email reaches `sales@sekil.id` (verify with real test)
- [ ] Confirmation email reaches user inbox (not spam)
- [ ] Rate limiting active (kalau implement — V1 minimal pakai Turnstile)
- [ ] Database connection pool size appropriate

### 15.2 Launch Day

- [ ] DNS propagation verified dari multiple region (`dig sekil.id +short` dari Singapore, US, EU)
- [ ] SSL cert active (https:// works tanpa warning)
- [ ] Vercel deployment marked production
- [ ] Final smoke test: home → produk → demo form submit → email arrives
- [ ] Monitor Sentry untuk 2 jam pertama (error rate < 1%)
- [ ] Monitor Vercel Analytics realtime
- [ ] Monitor GSC: submit homepage URL via "URL Inspection" → request indexing
- [ ] Submit ke Bing Webmaster Tools
- [ ] Sosial media: post launch announcement (LinkedIn primary, Instagram secondary)
- [ ] Email internal team + UNJANI: launched, brief

### 15.3 Post-Launch (Week 1–4)

**Week 1:**
- [ ] Daily check: error rate Sentry, form submit count, indexing progress GSC
- [ ] Reply demo request dalam SLA 24 jam — track conversion
- [ ] Fix any P0/P1 bug dalam 24 jam

**Week 2:**
- [ ] First content cadence kick-off (2 blog/minggu)
- [ ] First GSC keyword report — identify "almost-ranking" opportunities
- [ ] Adjust meta description untuk page dengan low CTR

**Week 3–4:**
- [ ] Start programmatic content batch generation (lihat doc 03)
- [ ] UNJANI batch review template + samples
- [ ] Begin Tier 2 programmatic publish

---

## 16. Maintenance & Operations

### 16.1 Weekly Tasks

- Monitor GSC: indexing status, CTR, top query
- Monitor GA4: traffic, conversion, source
- Monitor Sentry: error rate, new error type
- Review form submissions: quality, conversion to qualified lead
- Publish 2 blog post (cadence target)

### 16.2 Monthly Tasks

- Lighthouse audit all foundation page → fix regression
- Schema validation re-check
- Content audit: top 10 page by impressions — refresh content kalau perlu
- Backup database verification (download dump, test restore di local)
- Review Sentry error patterns → fix root cause

### 16.3 Quarterly Tasks

- Comprehensive SEO audit (lihat doc 02 section 10)
- Content strategy review: cluster performance, gap analysis
- Update programmatic content (salary, certification, jurusan)
- Re-review UNJANI templates kalau ada methodology update
- Security audit: dependency update, security headers, penetration test (V2 kalau scale)
- Performance budget review

### 16.4 Incident Response

**Tier 1 — Site down (production deployment broken)**:
1. Rollback via Vercel Dashboard (one-click ke previous deployment)
2. Investigate root cause di branch terpisah
3. Fix forward dengan new PR
4. Post-mortem doc di `docs/incidents/`

**Tier 2 — Form not submitting**:
1. Check Sentry untuk error
2. Check Turnstile dashboard
3. Check Resend dashboard untuk delivery issue
4. Manual test, fix, deploy hotfix

**Tier 3 — Slow page**:
1. Check Vercel Speed Insights
2. Identify slow page
3. Lighthouse audit → optimize
4. Deploy fix

---

## 17. Cost Projection (V1, monthly)

| Service | Tier | Cost |
|---|---|---|
| Vercel Hosting | Hobby (free) | $0 |
| Vercel Postgres | Free tier | $0 |
| Vercel Analytics | Free tier | $0 |
| Vercel Speed Insights | Free tier | $0 |
| Resend | Free tier (3000 email/bulan) | $0 |
| Cloudflare Turnstile | Free | $0 |
| Cloudflare DNS | Free | $0 |
| Sentry | Free tier (5k error/bulan) | $0 |
| Google Analytics 4 | Free | $0 |
| Google Search Console | Free | $0 |
| Anthropic API (content gen, ~100 page) | One-time | ~$5 (Rp 80k) |
| Domain `sekil.id` | (already owned) | — |
| **Total monthly** | | **~$0** |

**Threshold yang trigger upgrade:**

- **Vercel Hobby → Pro** ($20/dev/bulan): kalau bandwidth >100GB/bulan atau team need (e.g., password protected preview)
- **Vercel Postgres Free → Pro**: kalau >256MB storage atau >60 compute-hours/bulan
- **Resend Free → Pro** ($20/bulan): kalau >3000 email/bulan
- **Sentry Free → Team** ($26/bulan): kalau >5k error/bulan (kemungkinan kecil)

V1 realistic estimate: **gratis sampai 6–12 bulan pasca-launch**, kemudian Vercel Pro = $20/bulan jadi line item utama.

---

## 18. Future Considerations (V1.5+)

Bukan urgent V1, tapi log untuk roadmap:

- **CMS (Decap CMS)**: git-based CMS untuk UNJANI edit content tanpa PR (Week 8–12 V1.5)
- **A/B testing**: PostHog free tier kalau traffic >10k/bulan
- **i18n full English**: prepare ke V2 untuk SEA expansion
- **Self-serve B2C checkout**: V1.5+ — butuh payment gateway (Midtrans/Xendit Indonesia)
- **Whitepaper download**: gated content lead magnet — V1.5
- **Video content**: founder/UNJANI interview embed — V1.5
- **Community/forum**: V2 — discussion platform untuk users
- **Mobile app deeplink**: V2 kalau ada native app
- **PWA**: optional, kalau mau offline reading blog/guide
- **Algolia search**: site-wide search V2 kalau page count >200

---

## 19. Open Items untuk Founder Sebelum Hari 1

Sebelum eksekusi bootstrap, founder harus siap:

1. **GitHub Actions secrets** — values ready: `DATABASE_URL` placeholder, `RESEND_API_KEY` placeholder (untuk CI dummy)
2. **Resend account & domain** — sign up + add DNS records start sekarang (24–72 jam propagasi)
3. **Vercel account** — sign up dengan GitHub login
4. **DNS access** — verify access ke `sekil.id` DNS (di Cloudflare atau registrar)
5. **Brand asset** — minimal logo SVG + 1 OG image siap di folder `public/images/logos/`
6. **UNJANI commitment** — schedule meeting Week 1 untuk align content review timeline
7. **Email accounts** — provision: `hello@sekil.id`, `sales@sekil.id`, `privacy@sekil.id` (di domain email provider)
8. **GA4 + GSC** — owned dengan Google account yang stabil (jangan personal account)
9. **Anthropic API key** — generate untuk content generation script

---

**End of Repo Setup Guide v1.0.**

Companion documents:
- `01_Landingpage_Spec_v1.md` — full page spec
- `02_SEO_AEO_GEO_Strategy.md` — SEO/AEO/GEO technical
- `03_Content_Production_Pipeline.md` — programmatic content workflow
- `04_Claude_Design_Handoff_Prompts.md` — page build prompts